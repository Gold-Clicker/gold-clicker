alert("je");
const codes = [
  "start"
];

function activateCode(text) {
  if(text === "start") {
    val += 100000000000;
  }
}


function codes() {
  const input = document.getElementById("inputCode");
  const text = input.value;
  
  activateCode(text);
}